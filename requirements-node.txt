# ============================================================
# Node.js / React 프론트엔드 스택
# Node 의존성은 package.json / package-lock.json 으로 관리됩니다.
# 설치: npm install
# 개발 서버: npm run dev
# 프로덕션 빌드: npm run build
# ============================================================
#
# 주요 의존성 (package.json 참조):
#   react          19.x
#   vite           8.x
#
# 환경변수 (.env 파일):
#   VITE_API_BASE  Go 백엔드 주소 (기본값: http://localhost:8080)
# ============================================================
