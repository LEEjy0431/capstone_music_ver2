# Windows + conda 환경에서 pip 으로 설치할 순수 파이썬 패키지 목록
# (numpy / scipy / pandas / pytorch / librosa 의존 바이너리 등은 environment-windows.yml 의 conda 로 미리 설치됨)
#
# 버전 핀 없이 두는 이유:
#   - macOS 에서 freeze 한 requirements.txt 의 pin 은 Windows 휠과 충돌하는 경우가 잦음
#   - piano-transcription-inference / music21 등 핵심 패키지가 의존성을 알아서 끌어옴
music21
librosa
soundfile
pretty_midi
piano-transcription-inference
mir_eval
torchlibrosa
