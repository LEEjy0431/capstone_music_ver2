[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA 라우팅 — React Router 경로를 index.html로 폴백
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
